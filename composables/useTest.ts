export default function useTest(id?: number) {
  // const { fetchTodoState, refreshAllState } = toRefs(useOptionFeeStore())

  // const todo = fetchTodoState.value({ id: 1 })
  // const todo2 = fetchTodoState.value({ id: 2 })

  return {
    // todo,
    // todo2,
    // fetchTodoState,
    // refreshAllState,
  }
}
